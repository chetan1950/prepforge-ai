"use client";
import Link from "next/link";
import {useRouter} from "next/navigation";
import {useState,type FormEvent} from "react";
import {ArrowLeft,ArrowRight,Check,Eye,EyeOff,Sparkles} from "lucide-react";
export function AuthForm({mode}:{mode:"login"|"register"}){const[busy,setBusy]=useState(false),[error,setError]=useState(""),[show,setShow]=useState(false);const router=useRouter();async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();setBusy(true);setError("");const data=Object.fromEntries(new FormData(e.currentTarget).entries());try{const res=await fetch("/api/auth/"+(mode==="login"?"login":"register"),{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(data)});const body=await res.json();if(!res.ok)throw new Error(body.error||"Please check your details.");router.push("/dashboard");router.refresh()}catch(err){setError(err instanceof Error?err.message:"Something went wrong. Try again.")}finally{setBusy(false)}}const signup=mode==="register";return <main className="auth-shell"><section className="auth-intro"><Link href="/" className="brand"><span className="brand-mark">p</span>prepforge<span className="brand-ai">ai</span></Link><div className="auth-story"><div className="eyebrow"><Sparkles size={14}/> PREPARATION WITH A CLEAR PURPOSE</div><h1>Your next opportunity<br/>starts with <span>one step.</span></h1><p>Build a steady practice, learn from what you try, and bring your best to every interview.</p><div className="auth-quote"><div className="quote-line"/><span>Made for the work behind your next opportunity.</span></div></div><span className="auth-foot">YOUR PLACEMENT JOURNEY, IN FOCUS</span></section><section className="auth-form-wrap"><Link href="/" className="back-home"><ArrowLeft size={15}/> Back to home</Link><form className="auth-form" onSubmit={submit}><div className="auth-form-icon"><Check size={18}/></div><div className="eyebrow">{signup?"YOUR NEXT CHAPTER":"WELCOME BACK"}</div><h2>{signup?"Create your account":"Pick up where you left off"}</h2><p>{signup?"Your preparation space is just a few details away.":"Sign in to continue your preparation."}</p>{signup&&<label>Full name<input required name="name" autoComplete="name" minLength={2} maxLength={80} placeholder="Your name"/></label>}<label>Email address<input required name="email" type="email" autoComplete="email" maxLength={254} placeholder="you@example.com"/></label><label>Password<div className="password-wrap"><input required name="password" type={show?"text":"password"} autoComplete={signup?"new-password":"current-password"} minLength={signup?10:1} maxLength={128} placeholder={signup?"At least 10 characters":"Enter your password"}/><button type="button" aria-label={show?"Hide password":"Show password"} onClick={()=>setShow(!show)}>{show?<EyeOff size={16}/>:<Eye size={16}/>}</button></div></label>{signup&&<div className="auth-extra"><label>College <span>OPTIONAL</span><input name="college" maxLength={120} placeholder="Your college or university"/></label><label>Graduation year <span>OPTIONAL</span><input name="graduationYear" type="number" min="2020" max="2040" placeholder="2027"/></label></div>}{error&&<div className="error-banner" role="alert">{error}</div>}<button className="button auth-submit" disabled={busy}>{busy?"Please wait…":signup?"Create my account":"Sign in"}{!busy&&<ArrowRight size={15}/>}</button><p className="auth-switch">{signup?"Already have an account?":"New to PrepForge?"} <Link href={signup?"/login":"/register"}>{signup?"Log in":"Create an account"}</Link></p></form><div className="auth-privacy"><span>Private by design</span><span>·</span><span>Your preparation data stays yours</span></div></section></main>}
